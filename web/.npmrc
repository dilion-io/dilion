# openapi-typescript 7.x declares a peer dependency on typescript ^5.x while this
# project is on typescript 6.x. The generator works fine with 6.x (it only uses the
# TS printer), so accept the peer range mismatch instead of pinning TS backwards.
legacy-peer-deps=true
